{
  "name": "health",
  "version": "0.1.0",
  "type": "module",
  "description": "WHOOP channel plugin for Claude Code. Recovery, sleep, and strain stream into Claude as events; Claude becomes your health instructor.",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "daemon": "bun run src/daemon.ts",
    "setup": "bun run setup/onboarding.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  }
}
